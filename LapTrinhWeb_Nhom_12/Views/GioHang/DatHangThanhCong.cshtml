@{
    ViewBag.Title = "Đặt hàng thành công";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card border-0 shadow-sm rounded-3 text-center p-5">

                @* --- Icon thành công có hiệu ứng --- *@
                <div class="mb-4">
                    <div class="success-icon-wrapper">
                        <i class="bi bi-check-circle-fill text-success" style="font-size: 5rem;"></i>
                    </div>
                </div>

                <h2 class="fw-bold text-success mb-3">Đặt hàng thành công!</h2>

                <p class="text-muted mb-4">
                    Cảm ơn bạn đã tin tưởng và mua hàng tại <strong>MedForAll</strong>.<br>
                    Đơn hàng của bạn đang được xử lý và sẽ sớm được giao đến bạn.
                </p>

                <div class="alert alert-light border border-dashed mb-4">
                    <small class="text-muted">
                        <i class="bi bi-info-circle me-1"></i>
                        Một email xác nhận đã được gửi đến hòm thư của bạn.
                        Vui lòng kiểm tra cả hộp thư Spam nếu không thấy.
                    </small>
                </div>

                <div class="d-grid gap-2 d-sm-flex justify-content-center">
                    <a href="@Url.Action("TrangChu", "TrangChu")" class="btn btn-primary px-4 py-2 fw-bold rounded-pill">
                        <i class="bi bi-cart-plus me-2"></i> Tiếp tục mua sắm
                    </a>

                    @* Chỉ hiện nút xem đơn hàng nếu người dùng đã đăng nhập *@
                    @if (Session["User"] != null)
                    {
                        <a href="@Url.Action("DonHang", "TaiKhoan")" class="btn btn-outline-secondary px-4 py-2 fw-bold rounded-pill">
                            <i class="bi bi-file-earmark-text me-2"></i> Đơn hàng của tôi
                        </a>
                    }
                </div>

            </div>
        </div>
    </div>
</div>

